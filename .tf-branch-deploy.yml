default-environment: dev

production-environments: dev # As we have only one env. Add more envs if required

defaults:
  var-files:
    paths:
      - ./terraform/config/terraform_config.tfvars

environments:
  dev:
    working_directory: ./terraform/modules
    var-files:
      paths:
        - ./terraform/config/terraform_config.tfvars
